const setup=id=>document.querySelectorAll(`#${id} button`).forEach(b=>b.onclick=()=>{document.querySelectorAll(`#${id} button`).forEach(x=>x.classList.remove('active'));b.classList.add('active')});setup('modes');setup('durations');document.querySelector('#generate').onclick=()=>{const p=document.querySelector('#prompt').value.trim(),s=document.querySelector('#status');if(!p){s.textContent='Please enter an instruction first.';return}s.textContent='Job submitted. Remote GPU worker routing will process the production pipeline.'};