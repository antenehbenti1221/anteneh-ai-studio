# Remote worker endpoint. Keep secrets out of the frontend.
GPU_WORKER_URL=https://your-worker.example/api/v1
GPU_WORKER_TOKEN=replace_me

# Object storage used for generated MP4s.
OUTPUT_BUCKET=videos

# Default production settings
DEFAULT_LANGUAGE=en
DEFAULT_ASPECT_RATIO=16:9
